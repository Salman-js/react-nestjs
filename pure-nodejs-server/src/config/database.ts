import { Options } from 'sequelize';

const dbConfig: Options = {
  database: 'con_account',
  username: 'root',
  password: '',
  host: 'localhost',
  dialect: 'mysql',
};

export default dbConfig;
